package ExerciciosLogica;//Escreve ao lado do código o que cada linha representa.

public class Exercicio14 {
    public static void main (String[] args){
/*
        int size = 27; //declara uma variável de tipo inteira chamada 'size' e lhe atribui o valor 27.
        String name = "Fido"; //declara uma variável do tipo String denominada 'name' e lhe atribui o nome "Fido".
        Dog myDog = new Dog(name, size); // cria o objeto chamado dog;
        x = size - 5; //declara uma váriavel 'x' e seu tamanho '-5'
        if (x < 15) myDog.bark(8);// se o número 'x' for menor que 15, o cachorro late 8x"

        while (x > 3){ //enquanto x for maior que 3
            myDog.play(); //meu cachorro brinca
        } //fecha a chave

        int[] numList = {2,4,6,8}; //declara uma variável inteira e uma lista dentro dela com os seguintes números 2,4,6,8;
        System.out.print("Hello"); //pede para imprimir a palavra "Hello";
        System.out.print("Dog: " + name); //Imprime o nome do cachorro concatenando
        String num = "8"; //declara uma String "num" com o número "8";
        int z = Integer.perseInt(num); //conversão do num String em num Int e declara como Z;
        try //tenta ler o arquivo abaixo

        {
            readTheFile("myFile.txt"); //ler o arquivo e o nome do arquivo;
        }
        catch(FileNotFoundException ex) //caso o arquivo não seja encontrado, executar a seguinte frase:

        {
            System.out.print("File not found."); //imprime a seguinte frase "File not found";
        }
        */
    }
}